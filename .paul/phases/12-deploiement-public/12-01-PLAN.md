---
phase: 12-deploiement-public
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - Dockerfile.front
  - docker-compose.prod.yml
  - backend/main.py
  - backend/.dockerignore
  - .dockerignore
  - deploy/README.md
autonomous: false
---

<objective>
## Goal
Déployer finance-pwa sur le VPS de Camille en **conteneurs Docker routés par Traefik** (comme ses ~28 autres apps), avec une URL publique HTTPS → compétence 7 de la grille B→A (+5).

## Purpose
Le VPS n'est PAS un serveur vierge : c'est un hôte multi-apps piloté par **Traefik** (réseau `traefik-public`, TLS Let's Encrypt/DuckDNS, middlewares sécu crowdsec/ratelimit/headers). On ne prend donc PAS les ports 80/443 ni un nginx dédié : on ajoute 2 conteneurs (backend + front statique) au réseau Traefik avec les labels de routage, sur un sous-domaine.
</purpose>

## Output
- `backend/Dockerfile` (FastAPI/uvicorn), `Dockerfile.front` (build Vite → nginx:alpine).
- `docker-compose.prod.yml` : services `finance-backend` + `finance-front`, réseau `traefik-public` externe, labels Traefik (Host sous-domaine, entrypoint websecure, certresolver).
- URL publique HTTPS fonctionnelle, documentée.
</objective>

<context>
@.paul/PROJECT.md
@backend/main.py
@src/config/env.ts
</context>

<vps_facts>
## État réel du VPS (relevé SSH 2026-07-02)
- Ubuntu 22.04, user `ubuntu`, sudo sans mot de passe. node 22 / python 3.10 / docker 29 / git / certbot présents.
- **Traefik** détient 80/443 (IP publique 92.222.229.148). Réseau des apps web : **`traefik-public`** (externe).
- Entrypoints : `web`(:80)→redirect→`websecure`(:443). Middlewares websecure : `crowdsec@file,sec-ratelimit@file,sec-headers@file`.
- Cert resolvers : **`letsencrypt`** (HTTP challenge) et **`duckdns`** (DNS challenge), email camille.douaud@ynov.com.
- Convention réseaux : `<app>_default` / `<app>_internal` par app + `traefik-public` pour l'expo.
</vps_facts>

<checkpoint type="human-action" blocking="true">
## Décisions requises de Camille AVANT exécution
1. 🔴 **Disque plein (100%, ~161 Mo libres)** — impossible de builder/pull sans libérer. Réclamable sans risque : `docker system prune` (build cache 1.66 Go + images dangling ~3.76 Go), `journalctl --vacuum-size=50M` (~150 Mo), `apt clean` (~196 Mo) ≈ **~6 Go**. → OK pour lancer ce nettoyage sûr ? (je ne touche PAS aux conteneurs/volumes actifs).
2. **Sous-domaine** : lequel pour l'app ? (ex `finance.<ton-domaine>` ou une entrée DuckDNS). Doit pointer vers le VPS. → nom + resolver à utiliser (`letsencrypt` ou `duckdns`).
Tant que 1 et 2 ne sont pas tranchés, ne pas exécuter.
</checkpoint>

<acceptance_criteria>
## AC-1 : images buildées, backend en conteneur
Given le disque libéré When on build Then `finance-backend` (uvicorn, host 0.0.0.0:3000 dans le conteneur, sans --reload) tourne et `curl` interne `/api/health` → ok.

## AC-2 : front buildé avec la bonne URL API (STD-005)
Given le build front When l'image `finance-front` est construite Then `VITE_API_URL=https://<sous-domaine>/api` est passé en build-arg (baked au build), servi par nginx:alpine, SPA fallback actif.

## AC-3 : routage Traefik + HTTPS
Given les labels Traefik When on ouvre `https://<sous-domaine>` Then Traefik route `/` → `finance-front` et `/api` → `finance-backend`, certificat TLS émis (letsencrypt/duckdns), middlewares sécu appliqués.

## AC-4 : parcours utilisateur en prod
Given l'URL publique When on s'inscrit/connecte + navigue Dashboard/Analyse/ETF Then tout fonctionne (auth, prédiction backtest, graphes).

## AC-5 : sécurité + persistance
Given la prod When on inspecte Then `SECRET_KEY` vient d'un `.env` non commité, CORS restreint au sous-domaine (plus `["*"]`), la base SQLite est sur un **volume Docker** (persiste au rebuild), stack redémarre au boot (`restart: unless-stopped`).
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Nettoyage disque sûr (après OK checkpoint #1)</name>
  <files>—</files>
  <action>Sur le VPS via SSH : `docker system prune -f` (dangling + build cache, PAS `-a`), `sudo journalctl --vacuum-size=50M`, `sudo apt-get clean`. Re-vérifier `df -h /` → viser >8 Go libres. Logger l'espace avant/après. NE PAS supprimer d'images/volumes utilisés.</action>
  <verify>`df -h /` montre l'espace libéré ; aucun conteneur actif arrêté.</verify>
  <done>Prérequis build satisfait.</done>
</task>

<task type="auto">
  <name>Dockerfile backend</name>
  <files>backend/Dockerfile, backend/.dockerignore</files>
  <action>Image `python:3.11-slim` (tag connu-bon, STD-004 : pas de digest inventé). `pip install -r requirements.txt`, copie du code, `CMD ["uvicorn","main:app","--host","0.0.0.0","--port","3000"]` (sans reload). `.dockerignore` exclut `data/*.db`, `__pycache__`, `.env`. La base SQLite ira sur un volume monté (voir compose).</action>
  <verify>`docker build backend/` OK ; conteneur répond `/api/health`.</verify>
  <done>AC-1.</done>
</task>

<task type="auto">
  <name>Dockerfile front (multi-stage) + CORS backend</name>
  <files>Dockerfile.front, .dockerignore, backend/main.py</files>
  <action>Multi-stage : `node:22-alpine` → `ARG VITE_API_URL` + `npm ci && npm run build` (STD-005) ; stage final `nginx:alpine` servant `dist/` avec conf SPA (`try_files ... /index.html`). Backend : CORS `allow_origins` configurable via env `FRONTEND_ORIGIN` (défaut `*` en dev, sous-domaine en prod) — STD-002.</action>
  <verify>`docker build -f Dockerfile.front --build-arg VITE_API_URL=https://<sous-domaine>/api .` OK ; page servie.</verify>
  <done>AC-2, AC-5 (CORS).</done>
</task>

<task type="auto">
  <name>docker-compose.prod.yml + labels Traefik</name>
  <files>docker-compose.prod.yml</files>
  <action>
2 services :
- `finance-backend` : build backend/, `env_file: backend/.env`, volume `finance-db:/app/data` (SQLite persistante), réseau `traefik-public` (externe) + réseau interne, `restart: unless-stopped`. Labels Traefik : `Host(<sous-domaine>) && PathPrefix(/api)` → port 3000, entrypoint websecure, certresolver <choisi>.
- `finance-front` : build Dockerfile.front avec build-arg VITE_API_URL, réseau `traefik-public`, `restart: unless-stopped`. Labels Traefik : `Host(<sous-domaine>)` (catch-all restant) → port 80, entrypoint websecure, certresolver <choisi>, `priority` inférieure au backend pour que `/api` gagne.
Réseau `traefik-public` déclaré `external: true`. Copier le style des labels d'une app existante (`docker inspect` d'un service routé) pour matcher middlewares/certresolver.
  </action>
  <verify>`docker compose -f docker-compose.prod.yml up -d` ; `https://<sous-domaine>/api/health` → ok depuis l'extérieur, cert TLS valide.</verify>
  <done>AC-3.</done>
</task>

<task type="auto">
  <name>Déploiement + vérif end-to-end + doc</name>
  <files>deploy/README.md</files>
  <action>Push la branche, `git clone/pull` sur le VPS (dans `/home/ubuntu/finance-pwa`), créer `backend/.env` prod (SECRET_KEY généré, FRONTEND_ORIGIN=sous-domaine), `docker compose -f docker-compose.prod.yml up -d --build`. Tester login + navigation depuis un navigateur externe. Documenter l'URL + la procédure dans `deploy/README.md` et le README racine + STATE.</action>
  <verify>Parcours complet OK depuis l'extérieur ; reboot → stack revient (`restart: unless-stopped`).</verify>
  <done>AC-4.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Les 28 conteneurs / apps existants, Traefik, crowdsec, les volumes/réseaux des autres apps.
- Ne JAMAIS `docker system prune -a` ni supprimer un volume/une image utilisés par une autre app.
- `.env` réels jamais commités ; pas de secret dans le repo ou les labels.
- Logique métier de l'app (auth, endpoints, backtest phase 11) inchangée.

## SCOPE LIMITS
- Déploiement Docker+Traefik uniquement (l'hôte impose ce pattern). Pas de bare-metal/systemd, pas de nouveau nginx sur 80/443.
- SQLite sur volume (pas de migration Postgres — hors-scope note). CI auto-deploy = plan ultérieur.
</boundaries>

<currency>
## Tech-currency & Orchestration
| Tool | Decision | Reason | Details |
|------|----------|--------|---------|
| /paul:deep-research | SKIP | pattern Docker+Traefik déjà en place sur l'hôte, labels copiables d'une app existante | — |
| /paul:workflow | SKIP | déploiement séquentiel, cible unique | — |
**Execution vehicle:** exécution SSH pilotée par le main thread (manipule credentials + serveur prod partagé — pas de sous-agent). Copier les labels Traefik d'une app routée existante.
</currency>

<verification>
1. `df -h /` : >8 Go libres après nettoyage.
2. `docker compose -f docker-compose.prod.yml ps` : 2 services up.
3. `curl https://<sous-domaine>/api/health` depuis l'extérieur → ok, TLS valide.
4. Navigateur externe : login + navigation complète OK.
5. `docker restart` d'un conteneur / reboot → l'app revient.
6. Les autres apps du VPS toujours en ligne (non impactées).
</verification>

<success_criteria>
- [ ] AC-1 à AC-5 satisfaits
- [ ] URL publique HTTPS live et documentée
- [ ] Compétence 7 grille B→A
- [ ] Aucune autre app du VPS impactée, aucun secret commité
</success_criteria>

<output>
SUMMARY à créer : .paul/phases/12-deploiement-public/12-01-SUMMARY.md
</output>
