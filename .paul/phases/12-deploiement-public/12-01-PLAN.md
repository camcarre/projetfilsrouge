---
phase: 12-deploiement-public
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - deploy/finance-backend.service
  - deploy/nginx-finance.conf
  - deploy/deploy.sh
  - backend/.env.production.example
  - .env.production.example
autonomous: false
---

<objective>
## Goal
Déployer l'app (backend FastAPI + frontend build) sur le VPS de Camille via SSH, avec une URL publique accessible → compétence 7 de la grille passe de B à A (+5).

## Purpose
Aujourd'hui l'app tourne seulement en local. La grille exige un déploiement réel prouvé. On met en ligne sur le VPS : uvicorn derrière Nginx (reverse proxy), frontend statique servi par Nginx, SQLite sur le disque du VPS, HTTPS si un domaine existe.

## Output
- Backend en service systemd persistant (redémarre au boot).
- Frontend buildé avec l'URL API de prod (STD-005) servi par Nginx.
- URL publique fonctionnelle (login + dashboard + analyse OK).
- Scripts de déploiement réutilisables dans `deploy/`.
</objective>

<context>
@.paul/PROJECT.md
@backend/main.py
@src/config/env.ts
</context>

<checkpoint type="human-action" blocking="true">
## Infos requises de Camille AVANT exécution
1. **VPS** : IP ou hostname.
2. **SSH** : utilisateur + méthode d'auth (chemin de la clé privée, ou mot de passe) + accès `sudo` (oui/non).
3. **Domaine** : nom de domaine pointant vers le VPS (pour HTTPS), OU déploiement sur IP en HTTP simple.
4. **OS** : Ubuntu/Debian supposé — confirmer.
5. **Préférence** : bare-metal (défaut, le plus simple) ou Docker.
Tant que ces infos manquent, ne pas exécuter les tâches (déploiement = action sur serveur réel).
</checkpoint>

<acceptance_criteria>
## AC-1 : backend en ligne et persistant
Given le VPS When on interroge `https://<domaine|IP>/api/health` Then réponse `ok` ; le service uvicorn tourne via systemd (host 127.0.0.1:3000, SANS --reload) et redémarre après reboot.

## AC-2 : frontend servi avec la bonne URL API (STD-005)
Given le build de prod When on ouvre l'URL publique Then le front charge et appelle `VITE_API_URL` = l'URL publique de l'API (baked au build), pas localhost.

## AC-3 : reverse proxy correct
Given Nginx When une requête arrive Then `/` sert `dist/`, `/api/*` proxifie vers `127.0.0.1:3000`. SPA fallback (`try_files ... /index.html`) actif pour les routes react-router.

## AC-4 : parcours utilisateur OK en prod
Given l'URL publique When on s'inscrit/connecte puis navigue Dashboard → Analyse → ETF Then tout fonctionne (auth, prédiction, graphes).

## AC-5 : sécurité de base
Given la prod When on inspecte la config Then `SECRET_KEY` vient d'un `.env` prod (jamais commité), CORS `allow_origins` restreint au domaine de prod (plus `["*"]`), HTTPS actif si domaine.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Durcir le backend pour la prod</name>
  <files>backend/main.py, backend/.env.production.example</files>
  <action>
- Rendre CORS `allow_origins` configurable via env (`FRONTEND_ORIGIN`), défaut `["*"]` en dev mais valeur du domaine en prod (STD-002 : lire l'env à la frontière). Ne pas casser le dev.
- Le bloc `if __name__ == "__main__": uvicorn.run(..., reload=True)` reste pour le dev ; en prod on lance uvicorn via systemd SANS reload (voir tâche service). Rien à changer ici si systemd appelle `uvicorn main:app` directement.
- Créer `backend/.env.production.example` documentant les clés : `SECRET_KEY`, `FRONTEND_ORIGIN`, `HF_API_TOKEN` (optionnel). PAS de vraies valeurs.
  </action>
  <verify>Backend démarre en local avec FRONTEND_ORIGIN défini ; CORS renvoie le bon header.</verify>
  <done>AC-5 (partiel : CORS + secret).</done>
</task>

<task type="auto">
  <name>Service systemd backend</name>
  <files>deploy/finance-backend.service</files>
  <action>
Créer une unit systemd : `ExecStart=<venv>/bin/uvicorn main:app --host 127.0.0.1 --port 3000` (SANS --reload), `WorkingDirectory=<repo>/backend`, `EnvironmentFile=<repo>/backend/.env`, `Restart=always`, `User=<ssh_user>`. Documenter en tête les chemins à adapter.
  </action>
  <verify>Sur le VPS : `systemctl enable --now finance-backend` → `systemctl status` actif ; `curl localhost:3000/api/health` → ok.</verify>
  <done>AC-1.</done>
</task>

<task type="auto">
  <name>Config Nginx (reverse proxy + SPA)</name>
  <files>deploy/nginx-finance.conf</files>
  <action>
Créer un vhost Nginx : `root <repo>/dist;` `location / { try_files $uri $uri/ /index.html; }` (SPA fallback), `location /api/ { proxy_pass http://127.0.0.1:3000; proxy_set_header Host $host; ... }`. Bloc HTTPS (certbot/Let's Encrypt) si domaine, sinon HTTP sur port 80. Commenter les valeurs à adapter (server_name, chemins).
  </action>
  <verify>`nginx -t` OK ; requête `/` sert le front, `/api/health` proxifie.</verify>
  <done>AC-3.</done>
</task>

<task type="auto">
  <name>Script de déploiement</name>
  <files>deploy/deploy.sh, .env.production.example</files>
  <action>
Script bash idempotent (paramétré par variables en tête : VPS_HOST, SSH_USER, DOMAIN, REPO_PATH) :
1. `npm ci && VITE_API_URL="https://<domaine|IP>/api" npm run build` (STD-005 : URL bakée au build).
2. `rsync` du repo (ou `git pull` côté VPS) + du `dist/` vers le VPS.
3. Côté VPS via ssh : créer venv backend + `pip install -r requirements.txt`, copier `.env`, installer l'unit systemd + le vhost Nginx, `systemctl restart finance-backend`, `nginx -s reload`.
4. (si domaine) `certbot --nginx -d <domaine>` pour le TLS.
Créer `.env.production.example` (front) documentant `VITE_API_URL`. STD-004 : si Docker choisi plus tard, ne jamais pinner un digest inventé.
  </action>
  <verify>Dry-run du script (echo des commandes) cohérent ; après run réel : URL publique répond.</verify>
  <done>AC-2, AC-4.</done>
</task>

<task type="auto">
  <name>Vérification end-to-end en prod</name>
  <files>—</files>
  <action>
Après déploiement : `curl https://<domaine|IP>/api/health`, ouvrir l'URL, tester inscription/login + navigation Dashboard/Analyse/ETF. Noter l'URL publique dans le README + STATE.
  </action>
  <verify>Parcours complet OK depuis un navigateur externe au VPS.</verify>
  <done>AC-4.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- La logique métier (auth, endpoints, calculs) — c'est du déploiement, pas du refactor.
- Les `.env` réels ne sont JAMAIS commités (seulement `.example`).
- Pas de secret en clair dans les scripts ou le repo.

## SCOPE LIMITS
- Bare-metal par défaut (uvicorn + Nginx + systemd). Docker seulement si Camille le demande.
- SQLite reste le stockage (pas de migration Postgres/Supabase — hors-scope pour la note).
- Pas de CI/CD auto-deploy dans ce plan (déploiement manuel via `deploy.sh` d'abord ; un job CI pourra suivre).
</boundaries>

<currency>
## Tech-currency & Orchestration
| Tool | Decision | Reason | Details |
|------|----------|--------|---------|
| /paul:deep-research | SKIP | stack déploiement classique (Nginx/systemd/uvicorn/certbot), bien connue | — |
| /paul:workflow | SKIP | déploiement séquentiel, une cible unique | — |
**Execution vehicle:** exécution SSH pilotée depuis le main thread une fois les accès fournis (checkpoint human-action). Pas de sous-agent (manipule des credentials).
</currency>

<verification>
1. `nginx -t` OK sur le VPS.
2. `systemctl status finance-backend` actif + `curl 127.0.0.1:3000/api/health` ok.
3. `curl https://<domaine|IP>/api/health` depuis l'extérieur → ok.
4. Navigateur externe : login + navigation complète OK.
5. Reboot VPS → service revient tout seul.
</verification>

<success_criteria>
- [ ] AC-1 à AC-5 satisfaits
- [ ] URL publique live et documentée
- [ ] Compétence 7 grille B→A
- [ ] Aucun secret commité
</success_criteria>

<output>
SUMMARY à créer : .paul/phases/12-deploiement-public/12-01-SUMMARY.md
</output>
