---
phase: 13-preuves-niveau-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/services/yahoo_etf_service.py
  - src/components/ui/MultiLineChart.tsx
  - src/components/ui/CorrelationHeatmap.tsx
  - src/pages/Etf/EtfPage.tsx
  - src/types/analytics.ts
  - docs/analyse-projet.ipynb
  - CLAUDE.md
autonomous: true
---

<objective>
## Goal
Renforcer les preuves des compétences 1, 3, 4, 6 de la grille pour tendre vers le niveau A, par des changements 100% autonomes (code + doc).

## Purpose
La grille (EVALUATION.md) place ces 4 compétences en B. Chacune monte d'un cran avec une preuve concrète : transparence du scoring reco (6), interactivité/accessibilité des graphes (4), EDA formalisée avec conclusions (3), cross-check d'une 2ᵉ source de données (1). Corriger aussi la doc CLAUDE.md (backend décrit à tort comme Express/JS).

## Output
- Reco ETF : breakdown du score (5 composantes) exposé + expliqué en UI.
- Graphes : tooltip + légende interactive (MultiLineChart), heatmap accessible.
- EDA : conclusions écrites + distribution des rendements dans le notebook.
- Data : source de secours si yfinance vide.
- CLAUDE.md corrigé.
</objective>

<context>
@.paul/PROJECT.md
@backend/main.py
@backend/services/yahoo_etf_service.py
@src/components/ui/MultiLineChart.tsx
@src/components/ui/CorrelationHeatmap.tsx
</context>

<acceptance_criteria>
## AC-1 (comp 6) : transparence du scoring reco
Given le profil investisseur When `GET /api/etfs/recommended` répond Then chaque ETF porte `match_score` ET `match_breakdown = { risk, horizon, esg, ter, goal }` (somme = match_score), et l'UI affiche ce détail (« pourquoi ce score »).

## AC-2 (comp 4) : graphes interactifs + accessibles
Given le comparateur ETF When on survole une courbe Then un tooltip montre la valeur ; la légende permet de cibler une série. La heatmap de corrélation a des `aria-label`/titres lisibles et une échelle de couleur légendée.

## AC-3 (comp 3) : EDA formalisée
Given le notebook `docs/analyse-projet.ipynb` When on l'ouvre Then il contient des cellules markdown de conclusions écrites (au moins 3 constats chiffrés) et un graphe de distribution des rendements.

## AC-4 (comp 1) : cross-check 2ᵉ source
Given yfinance renvoie vide/erreur pour un ticker When on récupère l'historique Then une source de secours est tentée (ex : Stooq via URL CSV publique) avant d'échouer ; le comportement est loggé, pas silencieux.

## AC-5 (doc) : CLAUDE.md exact
Given CLAUDE.md When on lit la section backend/stack Then elle décrit FastAPI (Python) + SQLite, pas Express.js/Node.

## AC-6 : aucune régression
Given les changements When on build/teste Then `npm run build`, `npm run lint` et `pytest -q` restent verts.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Breakdown du scoring reco (comp 6)</name>
  <files>backend/main.py, src/types/analytics.ts, src/pages/Etf/EtfPage.tsx (ou la page qui affiche les reco)</files>
  <action>
Refactorer `_compute_match_score` (main.py:1089) pour retourner un tuple `(total, breakdown)` OU ajouter `_compute_match_breakdown(etf, profile) -> dict` renvoyant `{ "risk": x, "horizon": x, "esg": x, "ter": x, "goal": x }` (mêmes règles que le score actuel, aucune valeur changée). Dans `get_recommended_etfs` (~815), ajouter `etf_dict["match_breakdown"] = breakdown`. Côté front : typer `MatchBreakdown`, et sous chaque ETF recommandé afficher le détail (5 lignes label + points, tabular-nums, palette existante) — un petit bloc dépliable ou une liste « Détail du score ».
  </action>
  <verify>curl la reco (avec profil) → chaque ETF a match_breakdown dont la somme == match_score. Build front vert, détail visible.</verify>
  <done>AC-1 satisfait.</done>
</task>

<task type="auto">
  <name>Interactivité + accessibilité des graphes (comp 4)</name>
  <files>src/components/ui/MultiLineChart.tsx, src/components/ui/CorrelationHeatmap.tsx</files>
  <action>
MultiLineChart : ajouter un `<Tooltip>` Recharts (si absent) et une `<Legend>` interactive (clic = toggle série) — utiliser les props Recharts natives, pas de lib nouvelle. CorrelationHeatmap : ajouter `aria-label` sur chaque cellule (ex `"Corrélation AAPL / MSFT : 0.72"`), un `<caption>`/titre accessible, et une petite légende d'échelle de couleur (rouge=négatif → emerald=positif). Respecter la palette neutral/emerald/red.
  </action>
  <verify>npm run build vert ; survol → tooltip ; clic légende → série masquée ; cellules heatmap ont aria-label.</verify>
  <done>AC-2 satisfait.</done>
</task>

<task type="auto">
  <name>Formaliser l'EDA (comp 3)</name>
  <files>docs/analyse-projet.ipynb</files>
  <action>
Ajouter au notebook : (1) une cellule de code qui calcule et trace la **distribution des rendements** d'un actif (histogramme matplotlib, `returns = closes.pct_change().dropna()`) ; (2) au moins 3 cellules/paragraphes markdown de **conclusions écrites chiffrées** (ex : volatilité annualisée observée, asymétrie des rendements, corrélation notable). Ne pas casser les cellules existantes. Si matplotlib absent, utiliser un fallback texte (describe()). Le notebook doit rester exécutable de bout en bout.
  </action>
  <verify>jupyter nbconvert --to script docs/analyse-projet.ipynb --stdout | head (pas d'erreur de parse) ; présence des cellules markdown de conclusions.</verify>
  <done>AC-3 satisfait.</done>
</task>

<task type="auto">
  <name>Source de secours (comp 1)</name>
  <files>backend/services/yahoo_etf_service.py</files>
  <action>
Dans la récupération d'historique, si yfinance renvoie vide/erreur, tenter une source de secours **Stooq** (URL CSV publique `https://stooq.com/q/d/l/?s=<ticker>&i=d`, via `requests`, parsée en DataFrame). Logger (STD-001) le passage en secours ; si les deux échouent → comportement d'échec actuel inchangé. Best-effort, borné (timeout court). Ne pas rendre le service dépendant du réseau pour les tests (garder le fallback mock existant).
  </action>
  <verify>Test manuel : simuler yfinance vide → la source Stooq est tentée (log visible). pytest existant reste vert.</verify>
  <done>AC-4 satisfait (cross-check partiel — refresh planifié hors-scope).</done>
</task>

<task type="auto">
  <name>Corriger CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Remplacer les mentions du backend « Express + JS » par la réalité : **FastAPI (Python) + SQLite**, `backend/main.py` (pas `server.js`). Corriger la ligne stack, la section Architecture backend, et le workflow « Ajouter un endpoint » (référencer `backend/main.py` + `backend/services/`). Ne pas toucher au reste.
  </action>
  <verify>rg -n "Express|server.js" CLAUDE.md → plus aucune occurrence trompeuse ; FastAPI mentionné.</verify>
  <done>AC-5 satisfait.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Les valeurs/règles du scoring (seulement exposer le breakdown, pas modifier les points).
- La palette (neutral + emerald + red), aucune nouvelle dépendance front/back (Recharts, matplotlib, requests déjà là).
- Le fallback mock ETF existant, les autres endpoints, la logique Monte Carlo/backtest de la phase 11.

## SCOPE LIMITS
- Comp 1 : cross-check de secours uniquement — PAS de refresh planifié/cron (hors-scope infra).
- Pas de refonte visuelle : on ajoute interactivité/accessibilité sur les graphes existants.
</boundaries>

<currency>
## Tech-currency & Orchestration
| Tool | Decision | Reason | Details |
|------|----------|--------|---------|
| /paul:deep-research | SKIP | libs déjà en place (Recharts, requests, matplotlib) ; Stooq = URL CSV publique stable | — |
| /paul:workflow | SKIP | 5 tâches bornées mais séquentielles, un seul APPLY suffit | — |
**Execution vehicle:** direct linear (APPLY sous-agent Sonnet) — 5 tâches back+front+doc.
</currency>

<verification>
1. `cd backend && python3 -m pytest -q` → vert.
2. `npm run build` (tsc + vite) → vert.
3. `npm run lint` → 0 warning.
4. curl reco → `match_breakdown` présent ; front affiche détail + tooltips + heatmap accessible.
5. `rg -n "Express|server.js" CLAUDE.md` → nettoyé.
</verification>

<success_criteria>
- [ ] AC-1 à AC-6 satisfaits
- [ ] Compétences 3/4/6 tendent vers A (preuves concrètes) ; comp 1 renforcée (cross-check)
- [ ] Doc projet exacte
- [ ] Aucune régression
</success_criteria>

<output>
SUMMARY à créer : .paul/phases/13-preuves-niveau-a/13-01-SUMMARY.md
</output>
