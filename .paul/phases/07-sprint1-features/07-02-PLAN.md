---
phase: 07-sprint1-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Etf/EtfPage.tsx
  - src/pages/Dashboard/DashboardPage.tsx
autonomous: false
---

<objective>
## Goal
Remplacer le Spinner de chargement ETF par des Skeleton cards, et améliorer les états vides sur la page ETF et le Dashboard avec des CTAs actionnables.

## Purpose
Actuellement la page ETF affiche un spinner centré pendant le chargement — pas cohérent avec le reste de l'app. Les états vides sont du texte nu sans action possible. Issue #3 du Sprint 1.

## Output
- `src/pages/Etf/EtfPage.tsx` : Skeleton list pendant chargement + état vide "Aucun résultat" avec bouton reset filtres
- `src/pages/Dashboard/DashboardPage.tsx` : welcome card non-connecté avec CTA "Se connecter"
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@src/pages/Etf/EtfPage.tsx
@src/pages/Dashboard/DashboardPage.tsx
@src/components/ui/Skeleton.tsx
</context>

<acceptance_criteria>

## AC-1: Skeleton ETF au chargement
```gherkin
Given la page ETF en cours de chargement
When les données ne sont pas encore arrivées
Then une liste de 4 skeleton cards s'affiche à la place du spinner
```

## AC-2: État vide ETF avec CTA reset
```gherkin
Given des filtres appliqués qui ne retournent aucun ETF
When la liste est vide
Then un message "Aucun ETF ne correspond à ces filtres" + bouton "Réinitialiser les filtres" est affiché
```

## AC-3: Welcome card Dashboard non-connecté
```gherkin
Given un utilisateur non connecté sur le Dashboard
When il n'y a pas de données portfolio
Then une card de bienvenue avec CTA "Se connecter" est affichée à la place du contenu vide
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>ETF page — Skeleton cards + état vide avec reset</name>
  <files>src/pages/Etf/EtfPage.tsx</files>
  <action>
    1. Remplacer le bloc `loading ? <Spinner>` par 4 skeleton rows :
       ```tsx
       loading ? (
         Array.from({ length: 4 }).map((_, i) => (
           <li key={i} className="flex items-center justify-between gap-3 py-3 px-4 -mx-4 rounded-xl border border-neutral-100 dark:border-neutral-800">
             <div className="flex-1 space-y-2">
               <Skeleton className="h-4 w-2/5" />
               <Skeleton className="h-3 w-3/5" />
             </div>
             <Skeleton className="h-6 w-12" />
           </li>
         ))
       )
       ```

    2. Améliorer l'état vide : remplacer le texte nu par un bloc avec bouton reset :
       ```tsx
       filteredEtfs.length === 0 ? (
         <li className="py-8 flex flex-col items-center gap-3 text-center">
           <p className="text-[13px] text-neutral-500 dark:text-neutral-400">
             Aucun ETF ne correspond à ces filtres.
           </p>
           <Button variant="secondary" onClick={resetFilters}>
             Réinitialiser les filtres
           </Button>
         </li>
       )
       ```

    3. Vérifier qu'une fonction `resetFilters` existe ou la créer — elle remet zone, sector, esg, terMax à leurs valeurs initiales.

    Avoid : modifier la logique de chargement ou les types ETF. Ne pas créer de nouveau composant.
  </action>
  <verify>
    - `npx tsc --noEmit` sans erreur sur EtfPage.tsx
    - Skeleton visible pendant 1-2s au chargement initial de /etf
    - Bouton "Réinitialiser les filtres" visible si filtres actifs + 0 résultats
  </verify>
  <done>AC-1 et AC-2 satisfaits</done>
</task>

<task type="auto">
  <name>Dashboard — welcome card non-connecté avec CTA</name>
  <files>src/pages/Dashboard/DashboardPage.tsx</files>
  <action>
    Localiser le bloc qui gère l'état non-connecté (actuellement texte "Connectez-vous" dispersé).
    Ajouter une card de bienvenue centrée en haut du Dashboard quand `!user && apiConfigured` :

    ```tsx
    {apiConfigured && !user && (
      <Card className="mb-5 text-center py-6">
        <p className="text-[14px] font-medium text-neutral-800 dark:text-neutral-100 mb-1">
          Bienvenue sur Finance PWA
        </p>
        <p className="text-[13px] text-neutral-500 dark:text-neutral-400 mb-4">
          Connectez-vous pour suivre votre portefeuille et recevoir des recommandations ETF.
        </p>
        <Link to="/auth">
          <Button variant="primary">Se connecter</Button>
        </Link>
      </Card>
    )}
    ```

    Placer ce bloc juste après le `<div>` ouvert du return, avant les autres cards.
    Vérifier que `Link` est déjà importé ou l'ajouter depuis react-router-dom.

    Avoid : modifier les cards de stats existantes, toucher à la logique de chargement.
  </action>
  <verify>
    - `npx tsc --noEmit` sans erreur sur DashboardPage.tsx
    - En mode non-connecté (/auth → pas de login) : welcome card visible en haut du Dashboard
    - En mode connecté : welcome card absente
  </verify>
  <done>AC-3 satisfait</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Skeleton ETF + états vides améliorés + welcome card Dashboard</what-built>
  <how-to-verify>
    1. `npm run dev:all`
    2. Page /etf — observer le chargement : skeleton cards pendant ~1s puis liste ETF
    3. Activer un filtre qui ne donne aucun résultat → bouton "Réinitialiser les filtres" visible
    4. Se déconnecter → aller sur / → welcome card "Bienvenue" avec bouton "Se connecter"
  </how-to-verify>
  <resume-signal>Type "approved" pour continuer, ou décris les problèmes à corriger</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/components/ui/Skeleton.tsx` — composant stable, ne pas modifier
- `src/services/` — pas de changement de logique API
- `backend/` — hors scope

## SCOPE LIMITS
- Pas de nouveau composant EmptyState (inline suffit)
- Pas de refactoring des pages au-delà des zones ciblées
- Pas d'animation custom (animate-pulse déjà dans Skeleton)

</boundaries>

<verification>
- [ ] `npx tsc --noEmit` sans erreur (hors format.test.ts pré-existant)
- [ ] Skeleton list visible pendant chargement ETF
- [ ] Bouton reset filtres visible sur 0 résultats
- [ ] Welcome card visible si non-connecté sur Dashboard
</verification>

<success_criteria>
- ETF page : skeleton pendant chargement, CTA reset sur vide
- Dashboard : welcome card avec CTA pour non-connecté
- Aucune régression sur les états existants
</success_criteria>

<output>
Après complétion, créer `.paul/phases/07-sprint1-features/07-02-SUMMARY.md`
</output>
